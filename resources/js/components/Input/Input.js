import React from "react";

const Input = ({ value }) => {
    return (
        <input
            className="border-b-2 flex justify-center  justify-items-center"
            value={value}
        />
    );
};

export default Input;
